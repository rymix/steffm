import styled from "styled-components";

const StyledLibraryIndex = styled.dl`
  font-size: 75%;
  overflow-y: scroll;

  dd {
  }

  dt {
    font-weight: bold;
  }
`;

export default StyledLibraryIndex;
