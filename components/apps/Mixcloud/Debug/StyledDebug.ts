import styled from "styled-components";

const StyledDebug = styled.div`
  font-size: 75%;
  overflow-y: scroll;

  dl {
    dt {
      font-weight: bold;
    }

    dd {
      margin-left: 2em;
    }
  }
`;

export default StyledDebug;
